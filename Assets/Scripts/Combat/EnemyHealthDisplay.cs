using System;
using RPG.Combat;
using TMPro;
using UnityEngine;
using UnityEngine.UI;

namespace RPG.Attributes
{
    public class EnemyHealthDisplay : MonoBehaviour
    {
       // Fighter fighter;
       // Health _health;
       // 
       // [SerializeField] private TMP_Text _text;
//
       // private void Awake()
       // {
       //     fighter = GameObject.FindWithTag("Player").GetComponent<Fighter>();
       //     _health = FindObjectOfType<Health>();
       //     _text = GetComponent<TMP_Text>();
       // }
//
       // private void Update()
       // {
       //     /*String.Format("{0:0}%", health.HealthPercentage()) kullanılma sebebi can azalırken olabildiğince az decimal number olarak gözükmesini sağlamak.
       //     (Decimal Number = %86.989 gibi gözükmesini istemiyoruz.) */
       //     if (fighter.GetEnemyHealth() == null)
       //     {
       //         _text.text = "N/A";
       //     }
//
       //      //_health = fighter.GetEnemyHealth();
       //     _text.text = String.Format("{0:0}%", _health.HealthPercentage());
       // }
    }
}